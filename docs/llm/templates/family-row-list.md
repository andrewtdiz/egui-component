# Family: Row List

Use this family for segmented controls, list rows, selectable rows, and compact collection UIs.

- Reuse `row_chrome` and `icon_label_row` before introducing new row layout math.
- Keep hover, pressed, and selected states token-driven.
- Prefer simple typed entries over generic row schemas.

# Family: Control

Use this family for direct inputs and actions.

- Resolve visual state locally with a private `resolve_*_style` helper.
- Reuse control chrome and row primitives before painting borders or focus states inline.
- Keep shorthand support minimal and centered on the dominant happy path.

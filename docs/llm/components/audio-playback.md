id: audio-playback
label: Audio Playback
family: composed
summary: Horizontal audio playback row with play/pause, waveform, and optional actions.

# Audio Playback

Describe the dominant use-case, the semantic fields that matter, and the primitives this component should compose.

## Family Guide

Read `family-composed.md` before editing this component.

## Notes

- Keep the runtime API typed.
- Keep playback state controlled by the caller.
- Keep the title outside the component.
- Keep the center waveform decorative only for now.
- Do not add an outer border in the default path.
- Prefer shared primitives over inline paint code.
- Keep the default path mechanical for both humans and agents.

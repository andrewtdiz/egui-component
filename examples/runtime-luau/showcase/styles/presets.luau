local presets = {}

presets.card = {
    width = 640,
    padding_x = 20,
    padding_y = 20,
}

presets.kicker = {
    tone = "Muted",
    size = 12,
}

presets.title = {
    weight = "Bold",
    size = 22,
}

presets.meta = {
    tone = "Muted",
}

presets.primary_button = {
    variant = "Primary",
    width = 120,
}

presets.secondary_button = {
    variant = "Secondary",
    width = 96,
}

presets.input = {
    width = 320,
    placeholder = "Enter a showcase title",
}

return presets
